import React, { FunctionComponent } from "react";

interface UseRefPageProps { };

const UseRefPage: FunctionComponent<UseRefPageProps> = () => {

    return (
        <div>

        </div>
    );
};

export default UseRefPage;
