import React, { FunctionComponent } from "react";

interface ContextPageProps { };

const ContextPage: FunctionComponent<ContextPageProps> = () => {

    return (
        <React.Fragment>
            <h4>You made it!</h4>
            <p>This is the context page</p>

        </React.Fragment>
    );
};

export default ContextPage;
