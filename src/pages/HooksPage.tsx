import React, { FunctionComponent } from "react";

interface HooksPageProps { };

const HooksPage: FunctionComponent<HooksPageProps> = () => {

    return (
        <div>

        </div>
    );
};

export default HooksPage;
